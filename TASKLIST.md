# SignalSpace — inventário de tarefas da frente backend

**Autoridade de execução:** [contrato local](docs/LOCAL_ADMIN_AUTHORIZATION.md), [MVP](docs/MVP.md), [PR #1](https://github.com/LuigiAPCPereira/SignalSpace/pull/1). O PR #1 permanece draft na branch `feat/m1-local-mcp-diagnostic`; a execução corrente ocorre em `codex/mvp-vertical-programming`, sem merge autorizado. Esta lista é autoridade para ID, estado, dependências, aceite e evidência; checkpoint é derivado. Revalidar HEAD/CI ao retomar. Estados: `pendente`, `em andamento`, `bloqueada`, `implementada não validada`, `validada`, `integrada` quando exigida, `cancelada`. **Provas distintas:** CI com cloudflared simulado comprova o harness; o smoke M3 na revisão `bd60fc3` é relato operacional do proprietário em 20/09/2026, não execução independente desta sessão. Sem logs brutos, IDs ou transcrição de chamadas anexados; não extrapolar além dos itens relatados.

**Reconciliação de ref em 23/09/2026:** a ref corrente desta frente é `codex/mvp-vertical-programming`, e o remoto live foi confirmado em `6b8adc5b44e4856a743f0df336241aff5b5da79e`. A base opt-in anterior está publicada; a fatia estrutural desta retomada será registrada em commits locais, sem push nesta missão. O PR #1 continua draft e sua HEAD/branch de origem continua `feat/m1-local-mcp-diagnostic`; ela não é a branch atual e não foi integrada nesta frente.

| ID | Marco | Tarefa e resultado | Estado | Dependências | Aceite/fonte | Evidência e limite | Branch/PR |
| --- | --- | --- | --- | --- | --- | --- | --- |
| SS-BE-001 | M1/M2 | Comprovar diagnóstico, `read_file`, `list_directory` e negações após revogação. | validada (smoke M2 relatado; evidência limitada) | OAuth e grant explícito | [MVP §§1–2](docs/MVP.md); [workspace](docs/WORKSPACE_SECURITY.md) | Relato anterior de leitura/listagem/revogação, sem logs individuais de listagem nem medição precisa do JWT. **Não repetido no smoke M3 com painel.** | backend, PR #1 |
| SS-BE-002 | M3 | Isolar 7676 público/7677 admin, tunelar só 7676 e falhar fechado em bind/serve. | **validada no escopo operacional de diagnóstico relatado; matriz de ataques/limites ainda em SS-BE-007** | SS-BE-003/004/005; SS-BE-008 composição; SS-BE-007 aceite negativo amplo | [Contrato §§1–2,8](docs/LOCAL_ADMIN_AUTHORIZATION.md); [guia](docs/QUICK_PANEL.md) | [`f7efbb3`](https://github.com/LuigiAPCPereira/SignalSpace/commit/f7efbb3f3fb5e04ae542140c15ecd79526e595f9), [CI #169](https://github.com/LuigiAPCPereira/SignalSpace/actions/runs/35536833997): reserva/isolamento; [`d6d03a8`](https://github.com/LuigiAPCPereira/SignalSpace/commit/d6d03a87df7d1f40527650bafbcb53e1cac69e52), [CI #176](https://github.com/LuigiAPCPereira/SignalSpace/actions/runs/35538258629): falha após prontidão/limpeza; [`98d06de`](https://github.com/LuigiAPCPereira/SignalSpace/commit/98d06deeaf58bafee3972814bae8c5549f065aa0), [CI #182](https://github.com/LuigiAPCPereira/SignalSpace/actions/runs/35539286668): OAuth entre listeners em harness. **Smoke real na revisão [`bd60fc3`](https://github.com/LuigiAPCPereira/SignalSpace/commit/bd60fc3c17f84261eedf02d5db0f6679ad29a227): proprietário relata isolamento das portas, túnel Cloudflare real, diagnóstico e encerramento/limpeza observados.** Sem logs/sondas externas independentes ou prova de todas configurações de proxy; testes negativos integrais permanecem SS-BE-007. | backend, PR #1 |
| SS-BE-003 | M3 | Pareamento, frase-senha, unlock, sessão, cookies, CSRF e derivação. | validada no componente, exercitada no Quick opt-in em teste; smoke de decisão admin relatado, auditoria/browser pendentes | SS-BE-002/008 para operação | [Contrato §§3–4](docs/LOCAL_ADMIN_AUTHORIZATION.md) | [`d4c8c3a`](https://github.com/LuigiAPCPereira/SignalSpace/commit/d4c8c3a9bf1c1d9236bafabddb6560d12a2dd158): Argon2id com sal, cap 64 sessões, 429 na quinta falha; [`1c73a7f`](https://github.com/LuigiAPCPereira/SignalSpace/commit/1c73a7f8f9f1778aee1a780fbe086dd07c7357ac): 401/403; [CI #134](https://github.com/LuigiAPCPereira/SignalSpace/actions/runs/35525837834) PASS. Pareamento 7677 testado nas CI #169/#182; relato M3 confirma decisão via API admin local, **não teste navegador/unlock**. | backend, PR #1 |
| SS-BE-004 | M3 | Máquina OAuth compartilhada terminal/painel, estados versionados e retidos, revalidação de grant, código só na conclusão. | validada no domínio e exercitada por decisão HTTP no Quick simulado; decisão OAuth real relatada | Contrato OAuth/grants; SS-BE-005 HTTP | [Contrato §§4–7](docs/LOCAL_ADMIN_AUTHORIZATION.md) | [`171fbc8`](https://github.com/LuigiAPCPereira/SignalSpace/commit/171fbc856677f6f7a2f8ed10b6bcc13322773af2), [CI #124](https://github.com/LuigiAPCPereira/SignalSpace/actions/runs/35524113001): decisão, retenção 10 min/64, concorrência. CI #182: decisão e `/authorize/complete` no mesmo processo Quick local; **M3 relatou decisão OAuth efetiva no fluxo real de diagnóstico**, sem atestar todos os estados ou emissão individual de código/token por logs. | backend, PR #1 |
| SS-BE-005 | M3 | GET fila/detalhe e POST decisão só em admin com sessão/CSRF/versões, erros e reconciliação. | validada no harness HTTP; decisão administrativa real relatada no smoke M3, GUI/negativos externos pendentes | SS-BE-003/004/002 | [Contrato §§4–7](docs/LOCAL_ADMIN_AUTHORIZATION.md) | [`ea8165c`](https://github.com/LuigiAPCPereira/SignalSpace/commit/ea8165c6b316f802c49baaa9874ef6cf97d47928), [CI #146](https://github.com/LuigiAPCPereira/SignalSpace/actions/runs/35531123085): OAuth real, 401/403/404/409/410 (410/404 fixture), versão/CSRF; [`98d06de`](https://github.com/LuigiAPCPereira/SignalSpace/commit/98d06deeaf58bafee3972814bae8c5549f065aa0), [CI #182](https://github.com/LuigiAPCPereira/SignalSpace/actions/runs/35539286668): decisão e conclusão através do Quick em harness. **Smoke M3 relatou API local e decisão efetiva, não todos os códigos de erro nem interação HTML.** | backend, PR #1 |
| SS-BE-006 | M3 | `/authorize/status` cookie+ID próprio, sem aprovação pública; CSP/fluxo em navegador. | **validada no escopo visual local descartável; HTTPS/Cloudflare real e emissão OAuth não revalidados nesta missão** | SS-BE-004; SS-BE-002/008; etapa de integração frontend autorizada | [Contrato §6](docs/LOCAL_ADMIN_AUTHORIZATION.md); [alinhamento UX](https://github.com/LuigiAPCPereira/SignalSpace/blob/3911c4eae2ec514ff5f30d904a993cf103f9be32/docs/FRONTEND_ADMIN_CONTRACT_ALIGNMENT.md) | [`30d5ca9`](https://github.com/LuigiAPCPereira/SignalSpace/commit/30d5ca9a1229459025e8a06f2cd77c7813c03bc2), [CI #160](https://github.com/LuigiAPCPereira/SignalSpace/actions/runs/35533172412): CSP/cookie/Host/Origin/no-CORS; [`0ed5f5a`](https://github.com/LuigiAPCPereira/SignalSpace/commit/0ed5f5a): primeira página OAuth com script same-origin; [`0988fc5`](https://github.com/LuigiAPCPereira/SignalSpace/commit/0988fc53101ef87a8f76c7b08865cf75d5cb872c), [CI #188](https://github.com/LuigiAPCPereira/SignalSpace/actions/runs/35548396262): script same-origin e testes funcionais Node. Em 22/09/2026, navegador integrado observou DENIED, EXPIRED, 403, 404, 429→PENDING, 503→PENDING, JSON malformado→PENDING e interrupção parcial de rede→PENDING; botão permaneceu desabilitado nos estados não aprovados e nenhum cenário negativo enviou POST. Em fixture local descartável, somente APPROVED permitiu clique; exatamente um POST foi recebido pelo stub local, sem emissão OAuth/callback/token, e o polling parou. Instrumentação temporária da fixture registrou zero erros JS, rejeições não tratadas, violações CSP ou console.error. Fixture/processo/aba foram encerrados/removidos; implementação do produto não precisou de correção. HTTPS/Quick Tunnel real, CI, grant ChatGPT Web, emissão OAuth real, integração/merge/deploy não foram exercitados. | `codex/mvp-vertical-programming`, PR #1 draft preservado sem merge |
| SS-BE-007 | M3 | Testes negativos Host/Origin, sessão, expiração, concorrência, grant revoke, resposta perdida, retenção e smoke. | **em andamento: smoke operacional de diagnóstico relatado; matriz negativa operacional integral não comprovada** | SS-BE-002…006 comportamento; SS-BE-008 composição | [Contrato §8](docs/LOCAL_ADMIN_AUTHORIZATION.md) | [`f474a8c`](https://github.com/LuigiAPCPereira/SignalSpace/commit/f474a8cd12cbe39c6af0f4fd968f64f39b3c707b), [CI #152](https://github.com/LuigiAPCPereira/SignalSpace/actions/runs/35532258138): socket perdido/GET, revoke; [`d6d03a8`](https://github.com/LuigiAPCPereira/SignalSpace/commit/d6d03a87df7d1f40527650bafbcb53e1cac69e52), CI #176: admin Serve cai/limpa; CI #182: OAuth completo com stub. **Smoke real M3 relatou diagnóstico correlacionado, `tools/list` autenticado, isolamento e limpeza; não executou/revelou evidência de ataques Host/Origin, expirados, revogação/read com painel, navegador ou UI.** | backend, PR #1 |
| SS-BE-008 | M3 | Iniciar API 7677 opt-in no Quick, nunca tunelar admin, validar smoke/limites. | **validada operacionalmente para diagnóstico no smoke M3 relatado; UI/navegador e matriz negativa fora do aceite observado** | SS-BE-002…005; SS-BE-007 para cenários negativos; SS-BE-006 para UI | [Contrato §§2,8](docs/LOCAL_ADMIN_AUTHORIZATION.md); [guia](docs/QUICK_PANEL.md) | [`f7efbb3`](https://github.com/LuigiAPCPereira/SignalSpace/commit/f7efbb3f3fb5e04ae542140c15ecd79526e595f9): modo opt-in/origem fixa, CI #169; [`d6d03a8`](https://github.com/LuigiAPCPereira/SignalSpace/commit/d6d03a87df7d1f40527650bafbcb53e1cac69e52): falha tardia, CI #176; [`98d06de`](https://github.com/LuigiAPCPereira/SignalSpace/commit/98d06deeaf58bafee3972814bae8c5549f065aa0): decisão OAuth em harness, CI #182. **Proprietário relata smoke M3 real em `bd60fc3` com túnel Cloudflare, decisão OAuth local e diagnóstico MCP correlacionado, isolamento/encerramento observados.** Sem HTML, logs integrais, navegador, merge ou teste de read nessa execução. | backend, PR #1 |

## Próxima seleção e fronteiras

### Atualização SS-BE-006 — commit `3deefc1`

- O gate versionado agora instala Node.js `22.14.0` e executa explicitamente `node --test internal/auth/consent_js_test.mjs`; os gates Go existentes foram preservados.
- Após `APPROVED`, `consent.js` continua consultando o servidor até receber `EXPIRED`, desabilita a conclusão durante estados de consulta inconclusivos e cancela o polling ao submeter. O navegador não calcula nem concede validade.
- O teste funcional Node cobre deterministicamente `APPROVED → EXPIRED` antes do clique e JSON inválido por falha de parsing. O teste Go confirma que `/authorize/complete` rejeita a aprovação expirada sem emitir código.
- Gates locais no HEAD `3deefc1`: formato, diff, `go test ./...`, `go test -race ./internal/auth ./cmd/signalspace`, `go vet ./...`, `go build ./...`, `node --check` e `node --test`: PASS.
- O monitoramento/consulta da CI remota não foi realizado conforme orientação do proprietário; portanto a cobertura CI deste novo SHA é **desconhecida**. A validação visual negativa e a submissão final continuam pendentes; nenhum túnel novo foi aberto.
- Reconciliação posterior: push normal concluído sem divergência; o remoto e o HEAD local apontam para `9e93b65a2e897cfb4c41539dd3fb65d3ccc576e1`. A diferença em relação ao remoto é vazia; os únicos itens fora do Git continuam sendo os dois patches preservados.
- O HEAD atual da branch é `66f2641`, commit somente documental de reconciliação; o código SS-BE-006 permanece no commit `3deefc1` e a documentação de gates em `9e93b65`.

**Smoke M3 real relatado como APROVADO pelo proprietário (revisão `bd60fc3`, 20/09/2026), restrito ao diagnóstico OAuth+MCP+API administrativa:** correlação `diagnosticID` ChatGPT/terminal, `tools/list` autenticado, portas isoladas e limpeza/encerramento observados. Esta sessão **não executou nem recebeu logs brutos desse smoke**; o relato não prova a matriz negativa completa, leitura M2 com painel, token/callback por evento ou segurança independente. Marcar SS-BE-002 e SS-BE-008 como validadas **nesse escopo**; SS-BE-007 permanece em andamento, SS-BE-006 está implementada não validada até o navegador.

**Próxima ação SS-BE-006:** completar, em ambiente HTTPS aprovado e descartável, a validação visual dos estados negativos e da expiração antes do clique; submeter `/authorize/complete` somente com autorização específica para o grant OAuth de teste. Depois decidir, com ownership/autorização próprios, se a integração visual da branch frontend `3911c4e` é necessária. Os protótipos `owner-pairing-flow.html` e `local-approval-flow-v2.html` continuam apenas referências, não código a copiar. **Não integrar branches, fazer merge ou deploy nesta atualização.** Percentual indeterminado sem denominador estável. Conector GitHub não vê worktree/stashes locais; conferir PR/HEAD e docs antes de continuar.

### Resultado visual atualizado — SS-BE-006 — 22/09/2026

A atualização acima foi supersedida no escopo da validação visual local descartável. A implementação do produto permaneceu sem alterações. O resultado detalhado, incluindo cenários, limites da fixture e ação seguinte, está no checkpoint de 22/09/2026 ao final de [`docs/PROJECT_STATE.md`](docs/PROJECT_STATE.md) e no registro correspondente de [`docs/SESSION_LOG.md`](docs/SESSION_LOG.md). A autorização para enviar relatórios ao ChatGPT Web não concede grant OAuth ao produto.

## Matriz consolidada SS-BE-007 — grupos 1–11

Reconciliada em 22/09/2026 contra os checkpoints e o histórico desta branch. A matriz do contrato ([`docs/LOCAL_ADMIN_AUTHORIZATION.md`, §8](docs/LOCAL_ADMIN_AUTHORIZATION.md)) separa cobertura automatizada local de evidência em listeners fixos, túnel ou navegador; nenhum teste simulado é contado como smoke externo.

| Grupo | Classificação | Evidência verificável e ambiente | Limite restante |
| --- | --- | --- | --- |
| 1. Listeners reais e isolamento público/admin | **CONFIRMADO no smoke descartável; matriz integral permanece parcial** | O checkpoint/commit `a2dd93b` registra o smoke real com destino exato `127.0.0.1:7676`, admin separado em `127.0.0.1:7677`, nove variantes administrativas externas retornando `404` sem `Location`, `Set-Cookie` ou corpo administrativo, metadata pública `200`, Host/Origin/proxy forjados negados e shutdown limpo. A cobertura de código permanece em `cmd/signalspace/quick_ports_test.go`: `TestQuickPreparedListenersServeIsolatedHTTPRouters` e `TestQuickPublicListenerRejectsAdministrativeMatrix`. | O smoke confirmou somente esta fatia descartável; não é a matriz integral de ataques, proxy/DNS rebinding, navegador, CI, merge ou deploy. SS-BE-007 não é concluída por esta evidência. |
| 2. 7677, Host/Origin, CORS e proxy | **parcialmente coberto** | `internal/admin/transport_test.go`: testes existentes e os novos `TestAdminTransportRejectsForgedProxyHeadersOverLoopback` e `TestAdminTransportProxyHeadersDoNotBypassSessionOrCSRF`; servidor HTTP loopback com Host/origem, preflight e `Forwarded`/`X-Forwarded-*` forjados, além de sessão/CSRF reais do `Gate`. `cmd/signalspace/quick_ports_test.go`: sockets isolados. | DNS rebinding, `Forwarded` real em proxy e host rewrite de implantação não foram exercitados; cabeçalhos foram variados diretamente no socket de teste. |
| 3. Pareamento, CSRF, segredo, reuso e bloqueio | **CONFIRMADO no escopo visual local descartável; externo parcial** | Além da automação em `internal/admin/credential_hardening_test.go`, `session_security_test.go`, `http_mutation_security_test.go` e `http_test.go`, a fixture visual local observou pair, `AUTHENTICATED`, lock, unlock inválido sem perder o bootstrap, unlock válido, reload e `401 AUTH_REQUIRED`, com UI real no navegador integrado. | Não é túnel/HTTPS externo, grant ao ChatGPT Web, workspace real, CI, merge ou deploy. |
| 4. Sessão autenticada e concorrência de pareamento | **coberto em automação local** | `internal/admin/session_test.go`, `session_security_test.go`, `http_test.go` e `http_response_loss_test.go`: sessão, troca/revogação, expiração, concorrência e resposta perdida após mutação real em listener loopback. Regressões Node em `internal/admin/ui/admin_js_test.mjs` confirmam GET `/session` sem replay de Pair/Unlock. | A falha de entrega é injetada no cliente após o handler concluir; não equivale a queda externa, navegador, túnel ou HTTPS operacional. |
| 5. Sessão revogada/expirada/bloqueada/restart | **CONFIRMADO no escopo HTTP + navegador local descartável; externo parcial** | `internal/admin/session_test.go`, `session_security_test.go`, `http_mutation_security_test.go`, `internal/auth/store_test.go` e `cmd/signalspace/quick_panel_oauth_restart_http_test.go` cobrem lock, refresh, CSRF, expiração, restart e descarte de estado; a fixture do grupo 10 confirmou no navegador integrado que o reload após shutdown não preserva sessão, fila, detalhe ou ações antigas. | Não é túnel/cloudflared, HTTPS operacional, grant ao ChatGPT Web, workspace real, CI, merge ou deploy. |
| 6. Concorrência, snapshot e metadados não confiáveis | **coberto em automação local** | `internal/admin/requests_test.go`, `internal/auth/decision_test.go`, `lifecycle_test.go` e `terminal_decision_test.go`: `expected_version`, campos forjados, decisões simultâneas, expiração e terminal. | Ambiente local controlado; não é prova de integração operacional externa. |
| 7. Grant de workspace e revogação | **coberto em automação local** | `cmd/signalspace/admin_oauth_revoke_test.go`, `terminal_decision_test.go`, `embedded_read_test.go`, `embedded_list_test.go`, `internal/mcp/workspace_*_test.go` e `internal/auth/read_scope_test.go`. | O smoke M3 real relatado não repetiu leitura/revogação com painel; nenhum túnel novo foi aberto. |
| 8. Página pública, cookie/ID e conclusão OAuth | **CONFIRMADO no escopo HTTP local; visual/externo parcial** | `internal/auth/session_isolation_http_test.go` (`TestOAuthHTTPSessionIsolationAndSingleLegitimateCompletion`) combina dois DCRs, dois `/authorize`, cookies/CSRF próprios, status cruzado negado, aprovação de A, três conclusões inválidas, uma conclusão legítima e replay sem segundo código em socket TCP real; `public_status_test.go`, `server_test.go` e `public_status_expiry_http_test.go` permanecem regressões. `consent_js_test.mjs` segue sendo cobertura DOM/fetch simulada, não prova de transporte. | O cookie `Secure` foi reapresentado manualmente pelo harness HTTP; isso não comprova entrega Secure em navegador/HTTPS. A matriz visual de estados negativos da página já foi registrada como confirmada em SS-BE-006; esta linha não a reclassifica como prova HTTP externa, callback seguido ou troca por token. |
| 9. Fila vazia, quotas, retenção e tombstones | **CONFIRMADO no escopo HTTP + navegador local com falhas injetadas; externo parcial** | `internal/admin/requests_expiry_http_test.go`, `requests_test.go`, `internal/auth/quota_test.go`, `public_status_test.go` e `lifecycle_test.go` cobrem limites, 410/404, 429 e retenção; a fixture real do grupo 9 confirmou no navegador uma fila PENDING real durante 503 `TEMPORARILY_UNAVAILABLE` e falha de transporte, sem tratar erro como vazio, seguida de recuperação do mesmo pedido. | 410/404 controlados e falhas de transporte injetadas não comprovam queda externa, proxy, Cloudflare, túnel ou HTTPS. Grupo 9 completo continua parcial. |
| 10. Reinício, bind e publicação fechada | **CONFIRMADO no escopo local HTTP + navegador integrado descartável; externo parcial** | `cmd/signalspace/quick_panel_oauth_restart_http_test.go` confirma duas composições reais, mesmo `StateDir`, shutdown/rebind, cookie administrativo obsoleto, descarte de pedidos/códigos transitórios e fila C nova. Na fixture temporária do grupo 10, a UI HTML/CSS/JavaScript real foi servida pelos listeners fixos `127.0.0.1:7676`/`localhost:7677`; na mesma aba/contexto do navegador integrado, pair → pedido PENDING → detalhe/botões → shutdown gracioso → reload mostrou UNPAIRED sem fila/detalhe/ações antigas → novo pair mostrou AUTHENTICATED com fila vazia. O teste terminou `PASS` e a captura visual final mostrou contador `0`. | Não houve túnel/cloudflared, HTTPS operacional, grant ChatGPT Web, workspace real, CI, merge ou deploy; não é aceite externo. |
| 11. Gates e smoke pertinente | **coberto em automação local; smoke externo pendente** | Checkpoints recentes registram gates locais sequenciais (`gofmt`, `git diff --check`, testes Go, race, vet, build, `node --check` e `node --test`) sem consulta de CI. O roteiro operacional está preparado, mas não foi executado nesta missão. | CI do HEAD atual é desconhecida; túnel, HTTPS, navegador externo e smoke operacional continuam separados. |

### Atualização SS-BE-007 — grupo 11, retry DNS do Quick e cancelamento — 22/09/2026

- A missão partiu do HEAD `f1f74d0d14a2897a329e214b3367653b8f59409c` na branch `codex/mvp-vertical-programming`. A ref foi revalidada com fetch explícito da branch, `git pull --ff-only origin codex/mvp-vertical-programming` e `git ls-remote`; após a publicação, HEAD e `origin/codex/mvp-vertical-programming` apontam para `5f67b0d0712479d1e9fe4af019376d95fc8e7301`.
- O teste original `go test -race ./cmd/signalspace -run '^TestQuickTransportStopsWaitingForTunnelOrCancellation$' -count=1 -timeout=180s` passou uma vez em 1,016 s, mas a repetição limitada `-count=100` reproduziu a falha 4/100 no subteste `context`: `public HTTPS verification failed after DNS retry window, tunnel closed: lookup : no such host`, em vez de `context canceled`.
- A causa ficou confirmada no seletor de `awaitQuickTransport`: `readyCtx` é derivado de `ctx`, portanto cancelamento explícito deixa `ctx.Done()` e `readyCtx.Done()` prontos; a escolha podia devolver o último DNS como expiração da janela. A correção mínima está em `cmd/signalspace/quick.go`: ao receber `readyCtx.Done()`, `ctx.Err()` tem prioridade e preserva `context.Canceled`. A regressão em `cmd/signalspace/quick_retry_test.go` usa barreira para cancelar enquanto o verificador ainda está ativo, sem sleep.
- Commit substantivo: `5f67b0d` (`fix(quick): preserve cancellation after transient DNS`). Não houve refatoração ampla, alteração de janela, relaxamento TLS, repetição após cancelamento ou mudança de contrato de erros permanentes/túnel.
- Os quatro testes `TestQuickTransportRetriesDNSUntilReady`, `TestQuickTransportFailsClosedWithoutRetryOnContractError`, `TestQuickTransportDNSRetryDeadline` e `TestQuickTransportStopsWaitingForTunnelOrCancellation` passaram em serial e em `-race -count=100` (4,017 s). A suíte `go test ./... -count=1 -timeout=300s`, o race amplo solicitado, `go vet ./...`, `go build ./...`, `gofmt` e `git diff --check` passaram.
- Não houve CI, cloudflared, túnel, HTTPS operacional, navegador, grant ChatGPT Web, workspace real, OAuth repetido, merge ou deploy. Ao final não havia listener em 7676/7677 nem processo `cloudflared`.
- Estado: SS-BE-007 grupo 11 **CONFIRMADO no escopo automatizado local**; SS-BE-007 global **PARCIAL/em andamento**. SS-MVP-002 permanece **PARCIAL/em andamento** e `SS-MVP-002-PROMOTION-GATE-001` permanece **PENDENTE**. Não foi criado novo ID; a próxima seleção deve usar um ID existente da TASKLIST.

### Lacuna selecionada e resultado

- Grupo selecionado: **1**, isolamento do listener público contra rotas administrativas, por risco direto de exposição do painel.
- Revisão da correção: o bloqueio literal anterior cobria apenas `/api/admin/`; a matriz ampliada reproduziu `307` em `//api/admin/...`, `/api//admin/...`, `/api/./admin/...` e `/prefix/../api/admin/...` por normalização posterior do `http.ServeMux`.
- Correção mínima: `cmd/signalspace/main.go` inspeciona os segmentos já decodificados de `r.URL.Path` e rejeita qualquer segmento exatamente `admin` antes do `ServeMux`, com `404`. Isso cobre `/admin`, `/api/admin/v1/*`, barras duplicadas, `.`/`..`, nomes/barras codificados e métodos alternativos sem alterar os handlers públicos.
- Regressão em `cmd/signalspace/quick_ports_test.go`: quinze variantes administrativas exigem `404`, ausência de `Location` e ausência de `Set-Cookie`; `GET /mcp`, `GET /authorize` e `GET /token` continuam alcançando suas rotas públicas sem `404` ou redirecionamento. O servidor do teste expõe somente o listener público, portanto não há encaminhamento para 7677 nem handler administrativo registrado nessa fronteira.
- SS-BE-007 permanece **em andamento**: a fatia automatizada local foi ampliada, mas a matriz operacional, visual e externa não foi concluída.

### Atualização SS-BE-007 — grupo 2, Host/Origin e cabeçalhos de proxy

- A implementação de `internal/admin/transport.go` não foi alterada: ela continua exigindo `Host: localhost:7677`, recusando Origin externo/ausente em mutações e ignorando `Forwarded`/`X-Forwarded-*` como autoridade.
- `TestAdminTransportRejectsForgedProxyHeadersOverLoopback` usa servidor HTTP em loopback com handler sentinela. Combina Host inválido ou público, Origin cruzado, preflight e POST sem Origin com `Forwarded`, `X-Forwarded-Host`, `X-Forwarded-Proto` e `X-Forwarded-For` forjados; exige 403, ausência de chamada ao API e ausência de CORS. Host e origem válidos continuam passando a fronteira de transporte, inclusive sem credenciais de transporte.
- `TestAdminTransportProxyHeadersDoNotBypassSessionOrCSRF` usa o `Gate` real em loopback: pareamento válido continua possível com proxy forjado, mas refresh com CSRF errado retorna `403 ACCESS_DENIED` e sessão forjada retorna `401 AUTH_REQUIRED`. Cabeçalhos de proxy não substituem Host, sessão ou CSRF.
- Não foi encontrada falha de produto; a lacuna era de cobertura. A evidência permanece local/automatizada: não comprova DNS rebinding nem proxy real.

### Atualização SS-BE-007 — lacuna literal de Host canônico e grupo 10

- `TestAdminTransportRejectsForgedProxyHeadersOverLoopback` agora inclui a variante `invalid_host_forwarded_to_canonical`: a requisição envia `Host: evil.example`, enquanto `Forwarded` e `X-Forwarded-Host` forjados apontam para `localhost:7677`. A resposta continua `403`, sem alcançar o handler; isso fecha a afirmação específica sem alterar a produção.
- `TestQuickInstanceRestartDropsAdministrativeAndWorkspaceAuthorizations` exercita a composição local com OAuth persistente, `Gate` administrativo e `workspaceConsole`, sem túnel. Após `Close`, o cookie/CSRF administrativo retorna `ErrAccessDenied`, o descritor de workspace retorna `ErrClosed`, e uma nova composição começa `UNPAIRED` sem herdar a concessão de workspace. O teste complementa `internal/auth/store_test.go`; não duplica a prova de identidade OAuth, pedidos pendentes e códigos.
- A evidência é local e automatizada. Não comprova túnel real, navegador/rede, DNS rebinding ou proxy real; CI não foi consultada e permanece desconhecida. SS-BE-007 continua em andamento.

### Atualização SS-BE-007 — grupo 4, recuperação após resposta perdida — 22/09/2026

- `internal/admin/http_response_loss_test.go` adiciona uma integração HTTP real em `127.0.0.1:7677`, com o `Gate`, os handlers administrativos e uma rota de pedidos real. O transporte de teste deixa o servidor concluir a mutação e somente então simula a perda observada pelo cliente; não é proxy de produção.
- Pair e Unlock foram exercitados em duas variantes cada: resposta inteira descartada, sem `Set-Cookie`, e resposta com cabeçalhos/`Set-Cookie` entregues e corpo JSON truncado. Em ambos os casos o cliente executa apenas o POST deliberado e depois GET `/api/admin/v1/session`; não há replay automático.
- Quando a resposta inteira é perdida, a sessão administrativa não é presumida e o GET reconcilia `LOCKED` sem cookie administrativo. Quando `Set-Cookie` chega apesar do corpo perdido, o GET confirma `AUTHENTICATED` e a rota de pedidos retorna `200`; o cookie foi observado somente por nome/estado, nunca por valor.
- Cada cenário registrou exatamente uma tentativa de Pair ou Unlock. O servidor confirmou a mutação antes da falha injetada; o shutdown foi gracioso e o rebind de `7677` passou. As regressões Node cobrem rede perdida e JSON truncado para Pair/Unlock, com uma chamada de sessão e nenhum segundo POST.
- Não houve defeito de produção reproduzido; nenhuma correção de runtime foi necessária. Commit de testes: `b5baa99` (`test(admin): reconcile pair and unlock after lost responses`). SS-BE-007 grupo 4 fica **CONFIRMADO no escopo local integrado**; SS-BE-007 global permanece **em andamento**.
- Limites: falha controlada dentro do cliente HTTP local, sem túnel/cloudflared, HTTPS, navegador/grant ChatGPT Web, workspace real, CI, merge ou deploy. SS-MVP-002 continua **PARCIAL/em andamento** e `SS-MVP-002-PROMOTION-GATE-001` permanece **PENDENTE**.

## Reabertura do MVP — tarefas de programação e integração

As tarefas abaixo foram adicionadas nesta missão com IDs novos. A implementação local não altera SS-BE-006/SS-BE-007 nem torna seus estados históricos automaticamente concluídos.

| ID | Marco | Tarefa e resultado | Estado | Dependências | Aceite/fonte | Evidência e limite | Branch/PR |
| --- | --- | --- | --- | --- | --- | --- | --- |
| SS-MVP-001 | M5 | Integrar uma UI administrativa funcional ao backend local: sessão, pareamento, desbloqueio, fila, decisão, reconciliação, refresh explícito e lock. | validada no escopo local; integração de branch pendente | SS-BE-003/005 | `docs/FRONTEND_ADMIN_CONTRACT_ALIGNMENT.md` na ref `3911c4e`; LOCAL_ADMIN_AUTHORIZATION | `79a1a37`; `internal/admin/ui/`, `ui.go`, `ui_test.go`; Node comportamental 19/19 após correção de invalidação entre sessões, Go HTTP/CSP, race e isolamento público passaram. Browser local descartável confirmou pair → fila → detalhe → approve → lock e a matriz negativa M4; sem dados fictícios no produto. | `codex/mvp-vertical-programming`, sem PR; `feat/frontend-oauth-consent` preservada |
| SS-MVP-002 | M6 | Definir e promover, de forma opt-in, a fronteira de autorização para programação, separando owner/client/session e os escopos READ, WRITE e revisão Git; implementar filesystem tipado estrutural e registrar a direção de worktrees. | implementada e validada localmente nesta fatia; aceite operacional externo e push pendentes | SS-BE-003; SS-BE-007; SS-MVP-003…005 | `docs/WORKSPACE_SECURITY.md`; `docs/PROGRAMMING_TOOLS.md`; decisão SS-MVP-002-PROMOTION-GATE-001 | Promoção anterior em `e97aaf7`/`d1bff2c`/`6b8adc5`; dez tools tipadas em `internal/workspace`/`internal/mcp`, composição pública e testes de copy/move/delete com limites e cleanup. Gates locais serão registrados ao fechamento. `test.run`, shell, mutação Git, `apply_patch` e worktree funcional não foram promovidos/implementados. | `codex/mvp-vertical-programming`, sem PR |
| SS-MVP-003 | M6 | Implementar edição textual segura com caminho relativo, no-symlink, arquivo regular, limite, UTF-8, conteúdo esperado e negação após revoke. | validada no escopo local | SS-MVP-002 | `docs/PROGRAMMING_TOOLS.md`; `WORKSPACE_SECURITY.md` | `internal/workspace/edit.go`, `edit_test.go`; conflito, traversal, symlink, limite, permissões, concorrência, publicação atômica e revoke cobertos; commits `1db1ebd` e `9e26c19`. Escritor externo não é isolado. | `codex/mvp-vertical-programming`, sem PR |
| SS-MVP-004 | M6 | Implementar execução controlada apenas do teste predefinido da fixture, com timeout, saída limitada, código de saída e término observado. | validada no escopo local; composição MCP e OAuth isolados validados | SS-MVP-002/003 | `docs/PROGRAMMING_TOOLS.md`; §9 Runtime de AGENTS | `internal/programming/test_runner.go` e testes; `internal/mcp/test_runner.go` e `test_runner_integration_test.go`; `signalspace:test.run` independente, somente `go test ./...`, sem shell/args arbitrários, FD retido durante execução, estados de sucesso/falha/timeout/cancelamento e negativos de escopo/revoke. `2065480`/`oauth_programming_integration_test.go` adiciona DCR/PKCE/token real do emissor e chamada MCP com `NewStaticJWTVerifier`. Processo mantém privilégios do usuário; não é sandbox; entrypoint público permanece sem execução. | `codex/mvp-vertical-programming`, sem PR |
| SS-MVP-005 | M6 | Implementar revisão Git somente leitura distinguindo baseline e mudanças produzidas, sem stage/commit/push. | validada no escopo local; composição MCP isolada validada | SS-MVP-003 | `docs/PROGRAMMING_TOOLS.md`; §16 Git de AGENTS | `internal/programming/diff_review.go` e testes; `internal/mcp/git_review.go` e `git_review_integration_test.go`; escopo OAuth/local `signalspace:git.review` independente, status/diff limitados, neutralização de fsmonitor/textconv/configuração/ambiente, negativos de token/escopo/owner/client/session/args/revoke/non-repo, sem exposição pública. | `codex/mvp-vertical-programming`, sem PR |
| SS-MVP-006 | M6 | Integrar vertical descartável leitura → edição → teste → diff → revoke → deny. | validada no escopo local; OAuth/MCP real no harness | SS-MVP-002…005 | `docs/PROGRAMMING_TOOLS.md`; critérios da missão | `internal/mcp/oauth_programming_integration_test.go` percorre seleção/confirmação explícita de leitura+escrita+teste+Git, tokens DCR/PKCE separados e JWT do emissor verificado; baseline Git anterior à edição; `read_file` real cuja saída é a precondição de `replace_text` → teste aprovado → diff → revoke/deny. Cobre negativas de escopo, sessão, grant pendente e exposição pública. Não prova navegador, HTTPS/túnel, grant real ao ChatGPT Web ou CI. | `codex/mvp-vertical-programming`, publicada sem PR |

### Checkpoint SS-MVP-002 — composição fail-closed — 21/09/2026

**Estado:** SS-MVP-002 continua **PARCIAL/em andamento**; a composição de entrypoint/Quick agora admite somente `diagnostic` e `read`. O gate `SS-MVP-002-PROMOTION-GATE-001` permanece **PENDENTE**; nenhum escopo/ferramenta de escrita, execução ou Git foi promovido.

**Evidência:** `cmd/signalspace/composition.go` centraliza os modos e seus escopos/consoles permitidos. Parser, Quick e composição OAuth/MCP usam o tipo fechado; seleção inválida é rejeitada antes de efeitos, e o handler verifica a correspondência canônica do plano antes de criar estado OAuth. O modo de leitura expõe somente `connection_diagnostic`, `read_file` e `list_directory`; `panel` é apenas apresentação local. `composition_policy_test.go` verifica rejeição sem leitura de input, túnel/verificador/fábrica do painel ou criação de estado; `promotion_gate_test.go` exige listas exatas de scopes/tools. Suíte Go completa, race dos pacotes afetados, vet e build passaram no commit `e1c3cb0aa686148e0d0ab526fc978c9a3e49c0bc`, enviado normalmente e confirmado no remoto; CI não foi consultada.

**Limites:** não houve túnel real, HTTPS operacional, navegador/grant ao ChatGPT Web, workspace real, CI, merge ou deploy. Os dois patches não rastreados continuam preservados. A próxima ação continua condicionada a uma missão vinculada à TASKLIST; esta implementação não libera promoção remota.

### Atualização SS-MVP-001 / M4 — aceite visual residual

Na ref `75a1745cbf334bc269be019a4ecb67b4ef6dfd87`, a matriz visual local foi ampliada com fixture descartável somente em `http://localhost:7677`: 401 limpou pedidos e ações; 409 `STALE_REQUEST` reconciliou sem aprovação otimista nem repetição; `WORKSPACE_GRANT_REQUIRED` permaneceu como concessão separada; 503 e falha de rede não foram tratados como fila vazia; 404 e 410 foram apresentados distintamente. Firefox headless produziu viewports de 390×844 e 320×844, sem recorte observado no pareamento; o navegador integrado confirmou textos longos na fila, abertura do detalhe, foco no título e Tab até `Recusar`.

Foi corrigido o defeito reproduzido em que `blockedDecisions` sobrevivia à mudança de sessão e mantinha ações desabilitadas após novo desbloqueio; a regressão elevou `admin_js_test.mjs` para 19 testes. A comparação técnica concluiu: pareamento, desbloqueio, lista, detalhe, permissões, decisões, erros/reconciliação, responsividade e acessibilidade estão **incorporados funcionalmente, com adaptações ao contrato real**; a branch `feat/frontend-oauth-consent` continua apenas referência preservada, sem merge. Aceite Quick/ChatGPT Web, HTTPS externo e integração formal de branches permanecem pendentes; M4 permanece parcial.

### Atualização SS-BE-007 — grupo 7, leitura OAuth, painel e revogação

- `cmd/signalspace/read_panel_live_integration_test.go` adiciona uma única vertical integrada pelos sockets reais fixos `127.0.0.1:7676` e `localhost:7677`, sem túnel/cloudflared, TLS simulado ou cliente ChatGPT Web. A composição canônica `read` é criada uma vez; o teste reserva os listeners, inicia os servidores reais, executa shutdown gracioso e comprova que as portas são liberadas.
- O fluxo atravessa DCR, OAuth diagnóstico com PKCE para tornar o cliente elegível, `workspace request`/`approve` local, `workspace status` com o único escopo `signalspace:workspace.read`, novo consentimento OAuth READ, fila/detalhe administrativo, pareamento com código descartável, decisão autenticada com CSRF e `expected_version`, conclusão pública, troca única por JWT e chamadas MCP HTTP reais.
- A evidência inclui snapshot administrativo com cliente, escopo, versão, `ACTIVE` e sem raiz; aprovação administrativa sem `code`/`access_token`; isolamento cruzado entre listeners/cookies/bearer; `tools/list` exato (`connection_diagnostic`, `read_file`, `list_directory`); leitura e listagem da fixture controlada; JWT ainda verificável após `workspace revoke current`; negação genérica sem conteúdo; snapshot administrativo `COMPLETED`/`REVOKED`; e nova autorização READ recusada sem criar pedido ou token adicional.
- Validação desta fatia: teste focalizado, suíte `go test ./... -parallel=1 -count=1 -timeout=180s`, race dos pacotes afetados, `go vet ./...`, `go build ./...`, `gofmt -l cmd internal` e `git diff --check` passaram. SS-BE-007 permanece **em andamento**; a evidência é local em loopback e não comprova túnel, HTTPS operacional, navegador, CI ou grant real do ChatGPT Web.

### Atualização SS-BE-007 — grupo 5, expiração administrativa via HTTP loopback

- A ref foi reconciliada em `codex/mvp-vertical-programming` após `git fetch origin --prune` e `git pull --ff-only origin codex/mvp-vertical-programming`; a base observada era `8924eb3037fd56c99bf2c6fd7eafc4afcf80f975`. Os dois patches não rastreados do usuário permaneceram fora do Git e intocados.
- `internal/admin/http_session_expiry_test.go` adiciona um servidor HTTP real em loopback TCP efêmero, com `Host: localhost:7677`, origem canônica, cookies reais, CSRF real, `Gate`/handlers reais e a fixture de requests existente. O relógio é controlado diretamente no `Gate` pelo pacote de teste; não há espera de tempo real nem API de relógio em produção.
- O cenário de idle comprova que GET de sessão e GET de fila não renovam o prazo; no instante do vencimento, GET de sessão, GET de fila, decisão e refresh retornam `401 AUTH_REQUIRED`, não expõem o ID do pedido nem alteram a fixture, e a sessão seguinte permanece `LOCKED`.
- O cenário absoluto comprova refresh explícito válido em vários instantes, rejeição de CSRF inválido, preservação do prazo absoluto e cap do idle no absoluto. Após o deadline absoluto, fila, decisão e refresh continuam em `401 AUTH_REQUIRED`, sem reativação implícita ou mudança do pedido.
- Validação completa desta missão: `TestAdminHTTPSessionExpiryAndAbsoluteRefresh`, `gofmt -l cmd internal`, `git diff --check`, `go test ./... -parallel=1 -count=1 -timeout=180s`, race nos seis pacotes afetados, `go vet ./...`, `go build ./...`, `node --check internal/admin/ui/admin.js` e `node --test internal/admin/ui/admin_js_test.mjs` passaram. Nenhuma CI, porta fixa, túnel, HTTPS operacional ou ChatGPT Web foi usado.
- Correção de evidência: o relatório textual anterior do grupo 4 registrou incorretamente o segundo hash de patch. O hash recalculado do arquivo preservado é `0fddedf6ad7ea61751a1aeda2417d956b780dda6cdd670ebf8a444df1a4e48f2`; o primeiro permanece `02e9de3193f8e85389406fda3f843aa5837739746091ac575b86f3e1e0d4cd9b`. Nenhum patch foi modificado.
- Publicação: commit `a523f7912fe279f6afb3ab949a5380a7b214332e` foi enviado normalmente para `origin/codex/mvp-vertical-programming`; `git ls-remote` e o remote-tracking local confirmaram o mesmo SHA, sem divergência de diff.
- Estado: SS-BE-007 grupo 5 **CONFIRMADO no escopo local automatizado**; SS-BE-007 global **PARCIAL/em andamento**; SS-MVP-002 **PARCIAL/em andamento**; `SS-MVP-002-PROMOTION-GATE-001` **PENDENTE**. Limites: sem restart operacional completo com navegador/rede, túnel/HTTPS real, grant ChatGPT Web, CI, merge ou deploy.

### Atualização SS-BE-007 — grupo 9, tombstone OAuth real via HTTP

- A ref foi revalidada em `codex/mvp-vertical-programming` no HEAD `857d465daccb36e0c1fd8f2078478b9bf1cac21c`; `git fetch origin --prune` e `git pull --ff-only origin codex/mvp-vertical-programming` confirmaram a base sem divergência. Os dois patches não rastreados permaneceram intocados e fora dos commits.
- `internal/auth/public_status_expiry_http_test.go` cria `auth.Server` real com `StateDir` temporário, registra o cliente em `/register`, cria o pedido pelo `/authorize`, captura o cookie `signalspace_auth` e o `request_id` efetivamente retornados e serve `/authorize/status` e as rotas OAuth pelo mesmo servidor HTTP de loopback efêmero, com `Host: localhost:7676`.
- A fixture ajusta somente `pending.Expires` e `terminalRecord.retainUntil` sob `Server.mu`, como controle determinístico do tempo; não espera cinco minutos, não altera relógio global e não introduz clock em produção. O endpoint público foi a evidência principal.
- O fluxo observado é `PENDING` (HTTP 200, `no-store`) → `EXPIRED` (HTTP 200, tombstone real) → `EXPIRED` novamente sem alterar `expires_at` ou `retainUntil` → `404 {"error":"not_found"}` após a retenção. Polling não recria pending nem prorroga retenção.
- Conclusão tardia por `/authorize/complete` retornou 403 sem redirect; decisão real tardia retornou `ErrOAuthRequestExpired` enquanto retido e `ErrOAuthRequestNotFound` após limpeza. `server.codes` permaneceu vazio e nenhum cliente tornou-se elegível por token; não houve código/token novo.
- O mapeamento administrativo `410 REQUEST_EXPIRED`/`404` continua coberto separadamente por `internal/admin/requests_expiry_http_test.go` com fonte controlada; esta missão não declara que esse mapeamento usa a mesma instância OAuth real.
- Validação desta fatia: teste HTTP focalizado, regressões de lifecycle/public status/admin, `gofmt -l cmd internal`, `git diff --check`, `go test ./... -parallel=1 -count=1 -timeout=180s`, race em `internal/auth`, `internal/admin` e `cmd/signalspace`, `go vet ./...` e `go build ./...` passaram. Não houve túnel, navegador, HTTPS operacional, workspace real, CI, merge ou deploy.
- Estado: SS-BE-007 grupo 9 — fatia de tombstone público real **CONFIRMADA no escopo local**; grupo 9 completo **PARCIAL**; SS-BE-007 global **PARCIAL/em andamento**; SS-MVP-002 **PARCIAL/em andamento**; `SS-MVP-002-PROMOTION-GATE-001` **PENDENTE**.

### Atualização SS-BE-007 — grupo 10, reinício HTTP sem recuperar autorizações

- A ref foi revalidada em `codex/mvp-vertical-programming` no HEAD `78b76d1ff279a6ff1b0698603211ad60f13fe8e1`; fetch/prune e pull fast-forward confirmaram a base sem divergência. Os dois patches não rastreados foram recalculados antes e depois e permaneceram fora do Git.
- `cmd/signalspace/quick_panel_oauth_restart_http_test.go` cria duas composições `diagnostic` reais com `auth.Server`, `Gate`, handler MCP/OAuth, API administrativa e os listeners fixos `127.0.0.1:7676` e `127.0.0.1:7677`. Ambos os ciclos usam o mesmo `StateDir`, mas instâncias novas de OAuth/Gate; o teste observa shutdown, fechamento dos listeners e rebind antes do segundo ciclo.
- Na primeira composição, DCR preserva um `client_id`; A permanece PENDING; B é aprovado somente pela API administrativa, a decisão não emite credencial e a conclusão pública produz um authorization code não trocado. A fonte administrativa confirma B como COMPLETED.
- Após o restart, issuer e `kid` do JWKS permanecem iguais e o cliente DCR antigo é aceito. O novo painel começa UNPAIRED; o cookie administrativo antigo retorna 401 `AUTH_REQUIRED`. A e B retornam 404 públicos; conclusão tardia de A retorna 403 sem redirect; o código B retorna 400 `invalid_grant` nas duas tentativas, sem access token.
- Um pedido C criado com o `client_id` persistido recebe ID distinto e fica PENDING sem consentimento herdado. Após novo pareamento, a fila HTTP contém somente C. O listener público não expõe a API administrativa, o listener administrativo não expõe MCP e `tools/list` sem bearer retorna 401 sem anunciar ferramentas de escrita/execução; a lista exata de ferramentas diagnostic permanece coberta separadamente por `TestPublicCompositionsKeepWorkspaceWriteUnpublished`.
- Nenhum código, token, cookie, pairing code ou passphrase foi registrado no relatório. Não houve mudança de produção, novo mecanismo de persistência, túnel, cloudflared, HTTPS operacional, navegador, grant ChatGPT Web, workspace real, CI, merge ou deploy.
- Validação: teste focalizado, regressões `TestIdentitySurvivesRestartButRequestsDoNot`, `TestQuickInstanceRestartDropsAdministrativeAndWorkspaceAuthorizations` e `TestPublicCompositionsKeepWorkspaceWriteUnpublished`, `gofmt -l cmd internal`, `git diff --check`, `go test ./... -p=1 -parallel=1 -count=1 -timeout=180s`, race nos cinco pacotes afetados, `go vet ./...` e `go build ./...` passaram.
- Estado: SS-BE-007 grupo 10 — fatia de reinício HTTP local **CONFIRMADA**; grupo 10 completo **PARCIAL**; SS-BE-007 global **PARCIAL/em andamento**; SS-MVP-002 **PARCIAL/em andamento**; `SS-MVP-002-PROMOTION-GATE-001` **PENDENTE**.

### Atualização SS-BE-007 — grupo 3, fluxo visual local de pareamento, bloqueio e desbloqueio

- A missão foi executada na branch `codex/mvp-vertical-programming`, partindo do HEAD `07bd85fcee8b34c53effcdcc55ce6d7070144161`. A correção de frontend está em `internal/admin/ui/admin.js`, com regressão em `internal/admin/ui/admin_js_test.mjs`; o commit de código é `0efd64a` (`fix(admin): preserve locked UI after rejected unlock`). Os dois patches não rastreados do usuário permaneceram fora do Git e intocados.
- Foi usada uma fixture descartável com `admin.NewGate`, `Gate.HandlerWithRequests`, `admin.NewServer` e a UI HTML/CSS/JavaScript real em `http://localhost:7677/`, aberta no navegador integrado do Codex. O fixture usou pedido administrativo controlado e dados redigidos; pairing code, passphrase, cookies e credenciais não foram registrados.
- Evidência visual/HTTP observada: `UNPAIRED` → `POST /api/admin/v1/pair` `201` → `AUTHENTICATED` com fila disponível; `POST /api/admin/v1/lock` `200` → `LOCKED` sem fila e com formulário; desbloqueio inválido `POST /api/admin/v1/unlock` `403` → permanece `LOCKED`, mostra rejeição e mantém o formulário, seguido de um único GET de sessão `200`; desbloqueio válido `POST /api/admin/v1/unlock` `200` → `AUTHENTICATED` e fila; reload autenticado preserva a fila; novo lock e reload retornam `LOCKED`; GET de fila sem sessão retorna `401 AUTH_REQUIRED`.
- O defeito concreto reproduzido era a UI transformar uma rejeição conhecida de desbloqueio em `UNAVAILABLE`, escondendo o formulário e perdendo o bootstrap. A correção trata somente 403/429 dessa rota, reconcilia a sessão por GET e não repete o POST. O teste Node comprova uma única tentativa de desbloqueio e a recuperação do bootstrap.
- Validação: 22 testes Node, `node --check internal/admin/ui/admin.js`, `gofmt -l cmd internal`, `git diff --check`, suíte `go test ./... -p=1 -parallel=1 -count=1 -timeout=180s`, race nos pacotes afetados, `go vet ./...` e `go build ./...` passaram. Não houve erro visível de JavaScript durante o fluxo visual.
- O resultado é **CONFIRMADO no escopo visual local descartável** para esta fatia do grupo 3. Rate limit/concurrency e demais grupos continuam cobertos somente onde indicado por seus testes; SS-BE-007 global permanece **PARCIAL/em andamento**, SS-MVP-002 permanece **PARCIAL/em andamento** e `SS-MVP-002-PROMOTION-GATE-001` permanece **PENDENTE**. Não houve túnel/cloudflared, HTTPS operacional, grant ChatGPT Web, workspace real, CI, merge ou deploy. Após a limpeza não restaram listeners em 7676/7677 nem processo `cloudflared`.

### Atualização SS-BE-007 — grupo 6, decisões concorrentes sobre solicitações OAuth reais

- A missão foi executada na branch `codex/mvp-vertical-programming`, partindo do HEAD `1e12e888a9d350ad0c29f6d79305a5af653484cf`. O teste novo é `cmd/signalspace/oauth_concurrent_decisions_http_test.go`; o commit focado é `6976343ca3aa3b90de71720703285b96bd699322` (`test(auth): verify concurrent OAuth decisions over HTTP`). Os dois patches não rastreados permaneceram fora do Git, intocados e não aplicados.
- A fixture reutiliza a composição `DIAGNOSTIC` canônica, `auth.Server`/Gate reais, API administrativa real e sockets HTTP de loopback nos listeners público/admin `127.0.0.1:7676` e `127.0.0.1:7677`, com cliente sem proxy e timeout. Dois clientes distintos foram registrados por DCR real e deram origem aos pedidos OAuth A e B.
- A fila autenticada listou exatamente A e B, com IDs distintos, client IDs próprios, escopo `signalspace:diagnostic`, redirect URI registrada, versão 1, `PENDING` e `verified=false`. O detalhe HTTP de cada pedido confirmou os mesmos metadados e nenhum segredo do OAuth.
- Um payload dirigido a A com campos extras de B (`client_id`, `scope`, `redirect_uri`) recebeu `400 INVALID_REQUEST`; A e B permaneceram byte-semantically iguais nos snapshots. Uma decisão de A com `expected_version=2` recebeu `409 STALE_REQUEST` sem alterar estado.
- A corrida controlada por barreira entre approve HTTP e deny terminal produziu exatamente um vencedor: o perdedor recebeu `ALREADY_DECIDED` da máquina de estados real. A terminou na versão 2, com exatamente um status `APPROVED` ou `DENIED` coerente com o vencedor e `decided_at` presente. B permaneceu intacto durante a disputa; sua decisão posterior independente de deny terminou `DENIED`, versão 2, sem alterar A.
- As respostas de decisão não continham código ou token e `IssuedClients()` permaneceu vazio; não houve conclusão OAuth nem emissão de credencial. O shutdown da composição foi executado e o teste confirmou o rebind das duas portas.
- Validação: teste focalizado, race focalizado, `gofmt -l cmd internal`, `git diff --check`, suíte `go test ./... -p=1 -parallel=1 -count=1 -timeout=180s`, race de `cmd/signalspace`, `internal/admin`, `internal/auth`, `internal/mcp` e `internal/workspace`, `go vet ./...` e `go build ./...` passaram. Não houve túnel/cloudflared, navegador, HTTPS operacional, workspace real, CI, merge ou deploy; SS-BE-007 grupo 6 está **CONFIRMADO no escopo HTTP local**, SS-BE-007 global permanece **PARCIAL/em andamento**, SS-MVP-002 permanece **PARCIAL/em andamento** e o gate de promoção permanece **PENDENTE**.

### Atualização SS-BE-007 — grupo 9, saturação da fila e quota de status OAuth — 22/09/2026

- A missão partiu do HEAD `380969e94ac70120b332d37dd879edf53e9c663f` na branch `codex/mvp-vertical-programming`; o teste foi publicado no commit `6f1ce2d673dccdd1d56df45b2023757dfb4036ab` (`test(auth): cover OAuth quota recovery over HTTP`). Os dois patches não rastreados permaneceram fora do Git, intocados, com SHA-256 `02e9de3193f8e85389406fda3f843aa5837739746091ac575b86f3e1e0d4cd9b` e `0fddedf6ad7ea61751a1aeda2417d956b780dda6cdd670ebf8a444df1a4e48f2`.
- `internal/auth/quota_http_test.go` adiciona dois testes com `auth.Server` real, `StateDir` temporário, DCR/PKCE e transporte TCP loopback de `httptest.NewServer`, cliente sem proxy e `Host` canônico. `TestOAuthPendingQueueSaturationAndHTTPRecovery` cria exatamente `maxPending` (`32`) por `/authorize`, confirma IDs/cookies distintos e estado PENDING, recebe `503 temporarily_unavailable` sem cookie nem novo pedido, mantém a fila cheia após `DecideTerminal(..., false)`, conclui a recusa por `/authorize/complete` em `403` sem redirect/código e comprova a reutilização de uma única vaga sem alterar os demais pedidos.
- `TestOAuthPublicStatusQuotaResetsWithoutRenewingRequest` cria um pedido por HTTP e aceita exatamente 120 consultas de `/authorize/status`; a 121ª recebe `429 slow_down`, `Retry-After` e `Cache-Control: no-store`. A janela da quota é retrocedida somente sob `Server.mu`; a consulta seguinte volta a `200 PENDING`, conserva exatamente o mesmo `expires_at` e altera somente a quota dessa rota. Não foram criados códigos nem clientes emitidos.
- Validação: os dois testes focados e seu `-race`, `gofmt -l cmd internal`, `git diff --check`, `go test ./... -p=1 -parallel=1 -count=1 -timeout=240s`, race de `cmd/signalspace`, `internal/admin`, `internal/auth`, `internal/mcp` e `internal/workspace`, `go vet ./...` e `go build ./...` passaram. Não houve alteração de produção.
- Estado: a fatia de grupo 9 sobre saturação/quota está **CONFIRMADA no escopo HTTP local**; o grupo 9 completo e SS-BE-007 global permanecem **PARCIAIS/em andamento**. SS-MVP-002 permanece **PARCIAL/em andamento** e `SS-MVP-002-PROMOTION-GATE-001` permanece **PENDENTE**. Limites: sem túnel/cloudflared, HTTPS operacional, navegador/grant ChatGPT Web, workspace real, CI, merge ou deploy.

### Atualização SS-BE-007 — grupo 10, restart visual no navegador integrado — 22/09/2026

- A ref observada para o código foi `e652d72` na branch `codex/mvp-vertical-programming`; os dois patches não rastreados do usuário permaneceram fora do Git, intocados e não aplicados. A fixture de navegador foi temporária, removida após a execução, e não alterou produção, frontend ou CI.
- A fixture iniciou duas composições `diagnostic` reais com `embeddedHandlerWithWorkspace`, `auth.Server`, `admin.Gate`, handlers reais e listeners fixos `127.0.0.1:7676`/`localhost:7677`, usando o mesmo `StateDir` temporário e instâncias novas no segundo ciclo. Um DCR/PKCE real criou um pedido OAuth PENDING antes da observação.
- No mesmo contexto e na mesma aba do navegador integrado, o primeiro ciclo mostrou `UNPAIRED` → `AUTHENTICATED`, fila com exatamente um pedido `PENDING`, detalhe completo e ações `Recusar`/`Aprovar`; nenhuma decisão foi enviada. O shutdown foi gracioso, os listeners foram liberados e o rebind passou.
- Após reload da mesma aba, o cookie administrativo antigo não autenticou a segunda instância: a UI mostrou `UNPAIRED`, distinguiu sessão inválida e não mostrou fila, detalhe nem ações antigas. O segundo pareamento mostrou `AUTHENTICATED`, contador `0` e `Nenhuma solicitação encontrada no servidor`. A captura visual final confirmou o estado vazio sem ação de decisão.
- A execução da fixture terminou `PASS` em 59,67 s. O teste HTTP focalizado existente continuou passando antes da fixture. Não foi reproduzido defeito de produção, portanto o helper temporário foi removido.
- Estado: a fatia de restart/bind/publicação fechada do grupo 10 fica **CONFIRMADA no escopo HTTP + navegador integrado local descartável**; grupo 10 completo e SS-BE-007 global permanecem **PARCIAIS/em andamento**. Não houve cloudflared, túnel, HTTPS operacional, grant ChatGPT Web, workspace real, CI, merge ou deploy.

### Atualização SS-BE-007 — grupo 8, isolamento de sessões OAuth por HTTP real — 22/09/2026

- A missão partiu do HEAD `a456334fbe4e7842c81327085a3daf9fbf34cdd3` na branch `codex/mvp-vertical-programming`; o teste foi publicado no commit `6e929e9068888515b73d6145f9a25e008a826119` (`test(auth): verify OAuth session isolation over HTTP`). Os dois patches não rastreados permaneceram fora do Git, intocados, com SHA-256 `02e9de3193f8e85389406fda3f843aa5837739746091ac575b86f3e1e0d4cd9b` e `0fddedf6ad7ea61751a1aeda2417d956b780dda6cdd670ebf8a444df1a4e48f2`.
- `internal/auth/session_isolation_http_test.go` usa `auth.Server` real, dois registros DCR distintos (`Fixture A`/`Fixture B`), dois `/authorize` reais com PKCE, cookies/CSRF próprios e cliente HTTP sem proxy em TCP loopback. A + cookie A e B + cookie B retornam PENDING; combinações cruzadas e ausência de cookie retornam 403. Aprovar A deixa A APPROVED e B PENDING, sem código; cookie B continua sem acesso ao status de A.
- Três conclusões cruzadas — cookie B, cookie ausente e CSRF B — retornam 403 sem `Location`/code e não consomem A. A conclusão legítima com ID/CSRF/cookie A retorna 303 para o callback registrado, preserva `state`/`iss`, cria exatamente um authorization code em memória e marca A COMPLETED; B permanece PENDING. O replay retorna 403 e não cria segundo código. Nenhum token ou cliente `issued` foi criado.
- Validação: teste focado e race focado, regressões de status/conclusão/expiração, suíte Go serial, `gofmt -l cmd internal`, `git diff --check`, race de `internal/auth`, `internal/admin`, `internal/mcp` e composição pública selecionada, `go vet ./...` e `go build ./...` passaram. A execução ampla de race encontrou falha não relacionada em `cmd/signalspace/quick_retry_test.go:81` (`lookup : no such host` no cenário de contexto); por isso o gate amplo de race fica **PARCIAL**, não PASS.
- Estado: a fatia de isolamento/conclusão HTTP do grupo 8 está **CONFIRMADA no escopo HTTP local**; o grupo 8 completo e SS-BE-007 global permanecem **PARCIAIS/em andamento**. Não houve alteração de produção, navegador, HTTPS operacional, callback seguido, troca por token, túnel/cloudflared, workspace real, CI, merge ou deploy. SS-MVP-002 permanece **PARCIAL/em andamento** e o gate de promoção **PENDENTE**.

### Atualização SS-BE-007 — grupo 9, integração administrativa com OAuth real bloqueada no gate de tempo — 22/09/2026

- A missão foi reaberta em `codex/mvp-vertical-programming` no HEAD `2e86de81193e8acd25b045ab1957d8c27dbc9a59`; `git fetch origin --prune` e `git pull --ff-only origin codex/mvp-vertical-programming` confirmaram a ref sem divergência. Os dois patches não rastreados permaneceram fora do Git e intocados: `signalspace-oauth-read-scope.patch` = `02e9de3193f8e85389406fda3f843aa5837739746091ac575b86f3e1e0d4cd9b`; `signalspace-workspace-client-binding.patch` = `0fddedf6ad7ea61751a1aeda2417d956b780ebf8a444df1a4e48f2`.
- O gate de viabilidade foi **BLOQUEADO** antes de criar uma nova fixture: o `auth.Server` mantém `pending` e `terminal` privados (`internal/auth/server.go:145-156`), cria o prazo do pedido com `time.Now()` em `/authorize` (`internal/auth/server.go:612-615`) e suas consultas administrativas também usam `time.Now()` (`internal/auth/request_snapshot.go:79-108`). A transição/retensão real ocorre sob esse estado privado (`internal/auth/lifecycle.go:21-70`); não existe clock injetável ou operação pública de expiração.
- A prova pública real existente `TestPublicStatusRetainsRealExpiredRequestOverHTTP` passou e controla somente os campos privados dentro do pacote `auth`; a prova administrativa `TestAdminHTTPExpiredDecisionAndRetentionBoundary` passou, mas usa deliberadamente `expiredOAuthRequests`, uma fonte controlada. Nenhuma das duas, isoladamente, fecha a integração exigida.
- Não foi criada fixture/teste substituto. Esperar o TTL real, alterar TTL/relógio de produção, usar `unsafe`/reflexão/`go:linkname` ou adicionar endpoint público de teste violaria o prompt e as fronteiras do produto. Um teste externo que importe `auth` e `admin` não obtém acesso ao estado privado; mover o teste para o pacote interno `auth` criaria ciclo, pois `internal/admin` importa `internal/auth`.
- Regressões focadas executadas: `TestPublicStatusRetainsRealExpiredRequestOverHTTP`, `TestAdminHTTPExpiredDecisionAndRetentionBoundary`, regressões de snapshot/lifecycle/decisão/status público e `node --test internal/admin/ui/admin_js_test.mjs` — PASS, 22/22. A UI já distingue `EXPIRED`, `REQUEST_EXPIRED`, `REQUEST_NOT_FOUND`, `AUTH_REQUIRED` e fila vazia autenticada; não houve defeito reproduzido nem alteração de produção.
- Estado: a fatia de integração admin→mesmo `auth.Server` do grupo 9 está **BLOQUEADA** pelo controle determinístico de tempo; o grupo 9 completo e SS-BE-007 global permanecem **PARCIAIS/em andamento**. SS-MVP-002 permanece **PARCIAL/em andamento** e `SS-MVP-002-PROMOTION-GATE-001` permanece **PENDENTE**. Próxima ação: decidir/documentar uma superfície de clock exclusivamente de teste ou aceitar a limitação antes de reabrir esta fatia; não repetir o grupo 10 nem promover capacidades públicas.

### Atualização SS-BE-007 — grupo 9, integração administrativa desbloqueada por ponte de teste — 22/09/2026

- A missão foi executada na branch `codex/mvp-vertical-programming`, partindo de `8b697c8ec6aabe57ccf8f37ae51e66a941901e2e`. O commit de implementação/teste é `312ef64` (`test(auth): verify admin OAuth expiry over real server`); os dois patches não rastreados do usuário permaneceram fora do Git, intocados, com SHA-256 `02e9de3193f8e85389406fda3f843aa5837739746091ac575b86f3e1e0d4cd9b` e `0fddedf6ad7ea61751a1aeda2417d956b780ebf8a444df1a4e48f2`.
- `internal/auth/testtime_bridge.go` oferece somente sob a tag explícita `signalspace_testtime` as operações de teste `TestExpirePendingRequest` e `TestExpireTerminalRetention`. Elas exigem o ID exato, mantêm `Server.mu`, falham para ID ausente/fase incompatível e não criam tombstone, decisão, código, token ou cliente. Não houve alteração em `Config`, TTLs, `time.Now()` de produção, interfaces, rotas, MCP, entrypoints ou composição Quick; a build normal não inclui o arquivo.
- `cmd/signalspace/oauth_admin_real_expiry_integration_test.go` usa uma única composição real com `auth.Server`, `admin.Gate`, `HandlerWithRequests`, listeners HTTP de loopback, `StateDir` temporário, DCR e `/authorize` reais, cookies/CSRF reais e pareamento administrativo por HTTP. O fluxo observado foi `PENDING` → GET real `EXPIRED` retido → POST `410 REQUEST_EXPIRED` → retenção vencida pela ponte → GET/POST `404 REQUEST_NOT_FOUND`; a fila autenticada foi distinguida de `401 AUTH_REQUIRED`, e não houve cliente emitido, código ou token.
- Aceite: teste focalizado com tag e race focalizado passaram; a suíte de `cmd/signalspace` com tag passou. `go list` confirmou ausência da ponte sem tag e presença com tag. Sem tag passaram `go test ./... -p=1 -parallel=1 -count=1 -timeout=300s`, race serial dos seis pacotes afetados, `go vet ./...`, `go build ./...`, `gofmt -l cmd internal`, `git diff --check`, `node --check internal/admin/ui/admin.js` e 22/22 testes Node. O race amplo foi repetido serialmente após uma execução paralela inválida que colidiu nas portas fixas 7676/7677.
- Estado: a fatia admin→mesmo `auth.Server` do grupo 9 está **CONFIRMADA no escopo HTTP local com ponte opt-in de testes**; grupo 9 completo e SS-BE-007 global permanecem **PARCIAIS/em andamento**; SS-MVP-002 permanece **PARCIAL/em andamento**; `SS-MVP-002-PROMOTION-GATE-001` permanece **PENDENTE**; CI permanece **DESCONHECIDA**. Limites: sem túnel/cloudflared, HTTPS operacional, navegador/grant ChatGPT Web, workspace real, CI, merge ou deploy.

### Atualização SS-BE-007 — grupo 9, falha 503/rede no painel com fila OAuth real — 22/09/2026

- A ref foi revalidada em `codex/mvp-vertical-programming`, HEAD local/remoto `b738b32640d8188f294b4278564d9c2c3dd6f098`; os dois patches não rastreados permaneceram fora do Git, intocados, com SHA-256 `02e9de3193f8e85389406fda3f843aa5837739746091ac575b86f3e1e0d4cd9b` e `0fddedf6ad7ea61751a1aeda2417d956b780ebf8a444df1a4e48f2`.
- A fixture descartável usou `auth.Server`, `admin.Gate`, API administrativa real, UI HTML/CSS/JavaScript real, listeners `127.0.0.1:7676`/`127.0.0.1:7677`, `StateDir` temporário, DCR e `/authorize` reais. O cliente `SS-BE-007 restart client` deixou exatamente um pedido diagnóstico PENDING; a fixture temporária não foi commitada e foi removida após o uso.
- No navegador integrado em `http://localhost:7677/`, o pareamento levou a `AUTHENTICATED`, contador `1`, fila com um PENDING e detalhe aberto com `Recusar`/`Aprovar`. O wrapper transitório interceptou somente GET `/api/admin/v1/requests`: primeiro respondeu `503 TEMPORARILY_UNAVAILABLE`, depois fechou a conexão para simular falha de rede, sem tocar `auth.Server` ou a fila.
- Em 503, a UI manteve `AUTHENTICATED`, exibiu `O serviço está temporariamente indisponível (503)`, contador `n/d`, não exibiu `Nenhuma solicitação encontrada`, não apresentou contador `0` nem decisão; o detalhe aberto e suas ações foram removidos porque não estavam reconfirmados. Na falha de rede, exibiu `A conexão com o serviço local foi perdida.` com os mesmos invariantes. Nenhum POST `/decision` foi acionado: somente a ação explícita `Renovar sessão` disparou as novas consultas, e nenhum botão de decisão foi tocado.
- Removidas as interceptações, o GET real recuperou a fila com contador `1` e o mesmo pedido PENDING; a fonte OAuth continuou PENDING ao encerrar a fixture. Nenhuma aprovação/recusa ou emissão ocorreu. A aba e servidores foram fechados; não restaram listeners em 7676/7677 nem processo `cloudflared`.
- Não houve defeito reproduzido e nenhuma alteração de produção. Regressões focadas, `go test ./... -p=1 -parallel=1 -count=1 -timeout=300s`, race serial dos seis pacotes afetados, `go vet ./...`, `go build ./...`, `gofmt -l cmd internal`, `git diff --check`, `node --check internal/admin/ui/admin.js` e 22/22 testes Node passaram. A evidência não comprova queda externa, proxy, Cloudflare ou HTTPS.
- Estado: fatia visual/HTTP do grupo 9 **CONFIRMADA no escopo local injetado**; grupo 9 completo e SS-BE-007 global permanecem **PARCIAIS/em andamento**; SS-MVP-002 permanece **PARCIAL**; `SS-MVP-002-PROMOTION-GATE-001` permanece **PENDENTE**; CI **DESCONHECIDA**. Próxima ação vinculada: aguardar nova missão em SS-BE-007, sem repetir expiração, quotas ou restart.

### Atualização SS-BE-007 — smoke real de túnel e isolamento — 22/09/2026

- Executado em `codex/mvp-vertical-programming`, HEAD local/remoto `f14c92afe82bf6505090321135c74a895e69b605`. PR #1 continua draft em `feat/m1-local-mcp-diagnostic`; sem integração. Os dois patches do usuário permaneceram não rastreados, não aplicados e intocados, com SHA-256 preservados: `02e9de3193f8e85389406fda3f843aa5837739746091ac575b86f3e1e0d4cd9b` e `0fddedf6ad7ea61751a1aeda2417d956b780dda6cdd670ebf8a444df1a4e48f2`.
- Preflight: portas 7676/7677 livres, nenhum processo residual, `cloudflared 2026.9.1`, ambiente compatível, origem efetiva `http://127.0.0.1:7676` e admin `127.0.0.1:7677`. Foi usado somente `go run ./cmd/signalspace connect quick panel`; `PUBLICAR PAINEL` foi autorizado para esta execução única.
- Foram feitas 10 sondagens públicas sem cookies, sem redirects automáticos e sem bypass TLS. Nove negativas administrativas, incluindo variantes de caminho e método, retornaram `404`, sem `Location`, `Set-Cookie` ou resposta administrativa; `/.well-known/oauth-protected-resource` retornou `200` JSON esperado. O certificado HTTPS foi aceito normalmente.
- Localmente, `Host` externo, `Origin` cruzada e preflight cruzado retornaram `403`; `Forwarded`/`X-Forwarded-*` forjados não autorizaram rota protegida (`401`); bootstrap canônico retornou `200` sem registrar cookie/conteúdo. Não houve pareamento, OAuth, decisão, callback, workspace, leitura, escrita, execução, Git ou MCP.
- Ctrl+C encerrou o processo; não restaram listeners em 7676/7677 nem processos SignalSpace/cloudflared. O `StateDir` Quick foi removido pelo cleanup do entrypoint e os temporários de sondagem foram removidos. Nenhum critério de parada foi acionado.
- Resultado: isolamento de túnel/portas **CONFIRMADO operacionalmente no escopo descartável**. Estados preservados: SS-BE-007 **PARCIAL**, SS-MVP-002 **PARCIAL**, gate de promoção **PENDENTE**, CI **DESCONHECIDA**. Limites: sem pareamento/decisão OAuth, MCP, navegador, proxy/DNS arbitrário, CI, merge ou deploy.

### Atualização SS-MVP-002 — auditoria da fronteira pública — 22/09/2026

- Estado explícito: `SS-MVP-002` permanece **PARCIAL**; `SS-BE-007` permanece **PARCIAL**; `SS-MVP-002-PROMOTION-GATE-001` permanece **PENDENTE**; CI permanece **DESCONHECIDA**. Esta auditoria não promoveu `workspace.write`, `test.run` ou `git.review`.
- A trilha `CLI → composição → Quick → OAuth → verificador JWT → MCP → ferramentas` foi conferida contra os contratos e fontes vigentes. A composição pública aceita somente `diagnostic` e `read`; o painel não muda o modo; modos inválidos falham antes de OAuth/listeners/túnel; admin e MCP público permanecem em handlers/listeners separados; e `tools/list` não anuncia escrita, teste ou Git no entrypoint público. Owner, client, sessão e revogação continuam revalidados no grant local e na chamada.
- Lacuna reproduzida: em composição de leitura, um bearer somente com `signalspace:diagnostic` recebia `read_file` em `tools/list`, embora a chamada fosse negada por escopo. A regressão focal falhou antes do ajuste. A descoberta de leitura agora exige a verificação independente de `signalspace:workspace.read`; tokens de escrita, Git e teste não anunciam leitura. Os testes de integração foram reconciliados com essa independência.
- Validação após a correção: `go test ./internal/mcp -p=1 -parallel=1 -count=1`, `go test ./cmd/signalspace -p=1 -parallel=1 -count=1`, `go test -race ./internal/mcp -p=1 -parallel=1 -count=1`, `go vet ./internal/mcp ./cmd/signalspace`, `go build ./...`, `gofmt` e `git diff --check` passaram. Não foram iniciados SignalSpace, cloudflared ou túnel nesta missão.
- Limites: a auditoria confirma a fronteira de descoberta/autorização em testes locais, não a matriz operacional integral, navegador, CI, grant externo, workspace, merge ou deploy. Os dois patches não rastreados do usuário permaneceram fora do Git, não aplicados e intocados. Próxima ação: aguardar missão vinculada a ID existente, sem promover capacidades.

### Atualização SS-MVP-002 — regressão pública READ após fix de descoberta — 23/09/2026

- A base da missão foi revalidada em `codex/mvp-vertical-programming`, HEAD local/remoto `ac5b6ee47c50df71512f8ce1d9eca56e875dc95d`. PR #1, `feat/m1-local-mcp-diagnostic` e frontend não foram alterados; os dois patches não rastreados permaneceram preservados, não aplicados e fora do Git.
- `cmd/signalspace/promotion_gate_test.go` agora reutiliza a mesma composição pública `read` e o fluxo DCR/OAuth descartável para verificar: token somente diagnóstico anuncia exatamente `connection_diagnostic` antes do grant; o mesmo token continua com a mesma lista depois do grant; token com `signalspace:workspace.read` anuncia exatamente `connection_diagnostic`, `read_file` e `list_directory`; o modo `diagnostic` rejeita solicitação `workspace.read` e permanece somente diagnóstico. A lista READ não contém write/test/Git. O caso equivalente de token diagnóstico assinado com escopo adicional contra handler sem reader permanece coberto por `internal/mcp/TestWorkspaceToolRequiresExplicitConfigurationAndIdentityVerifier`.
- Não houve defeito de produção novo: o fix de `readToolAccess.advertise` em `ac5b6ee` foi exercitado na composição pública e passou. Nenhuma fixture nova, ferramenta, escopo, modo ou contrato foi criado.
- Validação: focos de promotion/READ/painel/vertical OAuth, `go test ./... -p=1 -parallel=1 -count=1 -timeout=300s`, race serial de `cmd/signalspace` e `internal/mcp`, `go vet ./...`, `go build ./...`, `gofmt` e `git diff --check` — PASS.
- Estado preservado: `SS-MVP-002` **PARCIAL**, `SS-BE-007` **PARCIAL**, `SS-MVP-002-PROMOTION-GATE-001` **PENDENTE**, CI **DESCONHECIDA**. Não houve túnel/cloudflared, workspace real, grant ao ChatGPT Web, CI, merge ou deploy.

### Atualização SS-MVP-002 — instruções MCP alinhadas ao escopo OAuth — 23/09/2026

- A auditoria partiu do HEAD local/remoto `0a774ca2a2b9f0e260b5a68f354bab393d8448ec` e publicou a correção de implementação em `5f6f2371c7e22047af1dbb767b2f5faf1a5917e6` (`fix: align MCP initialization with OAuth scope`). O remoto foi revalidado no mesmo SHA; o commit documental desta atualização virá depois sem alterar o comportamento.
- Lacuna concreta reproduzida em `internal/mcp`: com `WorkspaceReader` configurado, um JWT contendo apenas `signalspace:diagnostic` recebia em `initialize.instructions` a orientação de leitura, embora `tools/list` anunciasse somente `connection_diagnostic`. O teste focal falhou antes da correção com a mensagem de capacidade indisponível; não houve acesso a arquivo nem ampliação de autorização.
- Correção mínima: `initialize` agora deriva as instruções de `readAccess.advertise`, `gitAccess.advertise` e `testAccess.advertise`, alinhando a narrativa de capacidade à descoberta efetiva do token atual. A regressão positiva mantém as instruções de leitura para bearer com o escopo READ; `cmd/signalspace/promotion_gate_test.go` verifica a mesma correspondência na composição pública sem nova fixture.
- A trilha CLI → composição → Quick → OAuth → verificador JWT → handler MCP → ferramentas foi reaberta. A matriz resumida do grupo 1 continua reconciliada com o checkpoint `a2dd93b6`: registra nove variantes externas do smoke descartável, sem transformá-las em prova da matriz integral de ataques; nenhum histórico anterior foi reescrito.
- Estado: `SS-MVP-002` **PARCIAL**, `SS-BE-007` **PARCIAL**, `SS-MVP-002-PROMOTION-GATE-001` **PENDENTE**, CI **DESCONHECIDA**. Não houve promoção de write/test/Git, túnel/cloudflared, workspace real, grant ChatGPT Web, consulta CI, merge ou deploy; os dois patches não rastreados continuam fora do Git e intocados.

### Atualização SS-MVP-002 / SS-BE-007 — auditoria de prontidão da fronteira pública — 23/09/2026

- A missão recebeu como base `a2dd93b6ae3be89bd3871cada8c406ed139ac014`, mas a ref efetivamente revalidada foi `codex/mvp-vertical-programming` no HEAD local/remoto `47878563c360b6b84d6ce17c8121ea6b491fc912`; não houve rewind. O PR #1, `feat/m1-local-mcp-diagnostic` e `feat/frontend-oauth-consent` permaneceram intocados.
- A trilha `CLI → composição → Quick → OAuth → verificador JWT → handler MCP → ferramentas` foi auditada. `composition.go`, `main.go` e `quick.go` mantêm somente `diagnostic`/`read`; `panel` não altera a composição; planos inválidos falham antes de OAuth, listeners, túnel e efeitos; o admin usa handler/listener separado; o Quick publica somente `127.0.0.1:7676`, com `127.0.0.1:7677` reservado ao admin opt-in; não existe encaminhamento do público para 7677.
- A leitura dos testes existentes confirmou listas públicas exatas: diagnóstico somente `connection_diagnostic`; leitura `connection_diagnostic`, `read_file` e `list_directory`; `workspace.write`, `test.run` e `git.review` não aparecem nos entrypoints públicos. OAuth não substitui grant local: owner, cliente, sessão e escopo são revalidados; revogação impede a chamada posterior com JWT ainda válido.
- Não foi encontrada lacuna negativa nova com consequência de segurança demonstrável. O defeito de descoberta de leitura e o defeito de instruções de `initialize` já foram reproduzidos e corrigidos nas missões imediatamente anteriores; não foi criada refatoração ou nova fixture nesta auditoria.
- Validação focal desta auditoria: testes de composição, rejeição de plano, isolamento dos listeners, matriz pública administrativa, promoção READ, ferramentas MCP e escopos OAuth em `cmd/signalspace`, `internal/mcp`, `internal/auth` e `internal/admin` — PASS. Não houve mudança de produção; `git diff --check` é o gate documental desta atualização.
- O grupo 1 permanece reconciliado ao checkpoint `a2dd93b6`: nove variantes externas foram registradas como smoke descartável da fatia de isolamento, não como matriz integral de ataques. Não foram repetidos túnel, cloudflared, pareamento, decisão OAuth, workspace ou chamada MCP.
- Estado mantido: `SS-BE-007` **PARCIAL**, `SS-MVP-002` **PARCIAL**, `SS-MVP-002-PROMOTION-GATE-001` **PENDENTE**, CI **DESCONHECIDA**. Os dois patches não rastreados permanecem fora do Git, não aplicados e intocados, com os hashes já registrados. Próxima ação: aguardar missão vinculada a ID existente, sem promoção de capacidades.

### Atualização SS-MVP-002 — matriz combinada OAuth/MCP e step-up — 23/09/2026

- A missão informou `47878563c360b6b84d6ce17c8121ea6b491fc912`, mas a ref local/remota foi revalidada em `codex/mvp-vertical-programming`, HEAD `23c35b6c7510fe36748b430f1f092b22e6f3a219`; não houve rewind. PR #1, `feat/m1-local-mcp-diagnostic`, `feat/frontend-oauth-consent` e os dois patches não rastreados permaneceram preservados e intocados.
- A matriz combinada foi exercitada no handler OAuth/MCP real do harness: metadata de recurso é estática por composição; `initialize`, `tools/list` e `tools/call` são sensíveis ao bearer. Para `signalspace:diagnostic`, a composição pública continua somente diagnóstico. Na composição READ, bearer diagnóstico recebe step-up abstrato para `signalspace:workspace.read` sem interpretar argumentos nem consultar grant; bearer READ anuncia leitura/listagem, mas execução continua condicionada ao grant local. No harness, read/write/Git/test são escopos independentes e a chamada direta sem escopo responde somente o desafio da capacidade, sem iniciar backend.
- Lacuna concreta reproduzida: bearer somente `signalspace:diagnostic` com `workspace.write` configurado recebia em `initialize` a frase de diagnóstico, embora metadata/tools/list/tools/call reconhecessem a capacidade somente para o bearer write. Correção mínima em `internal/mcp/server.go`: `initialize` agora inclui a instrução de substituição de texto somente quando `writeAccess.advertise` é verdadeiro. O teste combinado em `internal/mcp/oauth_programming_integration_test.go` falhou antes e passou depois; não houve alteração de composição pública.
- Validação: `go test ./internal/mcp -p=1 -parallel=1 -count=1 -timeout=300s`, `go test ./cmd/signalspace -p=1 -parallel=1 -count=1 -timeout=300s`, race serial de ambos os pacotes, `go vet ./...`, `go build ./...`, `gofmt` e `git diff --check` — PASS. Não houve SignalSpace operacional, cloudflared, túnel, navegador, OAuth/grant externo, workspace real, CI, merge ou deploy; 7676/7677 permaneceram livres.
- Resultado da tarefa: a lacuna local de coerência foi corrigida e a matriz fica pronta para decisão, sem aprovação automática. Estados mantidos: `SS-MVP-002` **PARCIAL**, `SS-BE-007` **PARCIAL**, `SS-MVP-002-PROMOTION-GATE-001` **PENDENTE**, CI **DESCONHECIDA**. Próximo evento requerido: **PROMOTION GATE PRONTO PARA DECISÃO DO PROPRIETÁRIO, NÃO APROVADO AUTOMATICAMENTE.**

### Atualização SS-MVP-002 / SS-MVP-002-PROMOTION-GATE-001 — composição opt-in READ + WRITE + GIT — registro pré-publicação — 23/09/2026

- A decisão do proprietário foi registrada no ChatGPT Web: aprovar a promoção local opt-in para READ, WRITE e revisão Git observacional. Shell, comandos arbitrários, `test.run` e mutações Git ficaram explicitamente fora da aprovação e exigem contrato/gate separado. A base local e remota revalidada antes da implementação era `61863d6a6b1d50d34d1a73f989ea97a18360e8cd`; não houve rewind, push, merge ou deploy.
- O commit de implementação `e97aaf7ec84577f2999f0bb725db5d2b8a451ce2` (`feat(signalspace): promote opt-in read write git composition`) adicionou o modo fechado `connect quick programming [panel]`, `NewOAuthProgrammingHandler`, composição explícita de reader/lister/writer/revisor Git e baseline Git por sessão. O modo `diagnostic`/`read` não foi ampliado; o painel administrativo continua local em `7677`, separado do MCP público em `7676`.
- A matriz pública local foi exercitada no handler OAuth/MCP real: bearers com `signalspace:workspace.read` recebem `read_file`/`list_directory`; `signalspace:workspace.write` recebe `replace_text`; `signalspace:git.review` recebe `review_git_changes`. As chamadas revalidam owner, cliente, sessão, escopo e concessão corrente. `tools/list` e `initialize` não anunciam `run_workspace_tests` nem shell; não existe mutação Git. Revogação nega JWT ainda criptograficamente válido.
- Validação fresca: `go test ./cmd/signalspace ./internal/mcp -count=1`; `go test ./... -p=1 -parallel=1 -count=1 -timeout=300s`; `go test -race ./cmd/signalspace ./internal/mcp -p=1 -parallel=1 -count=1 -timeout=300s`; `go vet ./...`; `go build ./...`; `gofmt -l cmd internal`; `git diff --check` — PASS. A checagem de listeners/processos após os gates permanece obrigatória no fechamento do relatório.
- Estado explícito: `SS-MVP-002` **PARCIAL**; `SS-BE-007` **PARCIAL**; `SS-MVP-002-PROMOTION-GATE-001` **APROVADO PELO PROPRIETÁRIO / IMPLEMENTAÇÃO LOCAL CONFIRMADA**; CI **DESCONHECIDA**. O gate não comprova HTTPS, navegador, grant ao ChatGPT Web, workspace real, túnel ou aceite operacional externo.
- Preservação: PR #1, `feat/m1-local-mcp-diagnostic`, `feat/frontend-oauth-consent` e os dois patches não rastreados continuam fora do Git, não aplicados e intocados. SHA-256: `signalspace-oauth-read-scope.patch` = `02e9de3193f8e85389406fda3f843aa5837739746091ac575b86f3e1e0d4cd9b`; `signalspace-workspace-client-binding.patch` = `0fddedf6ad7ea61751a1aeda2417d956b780dda6cdd670ebf8a444df1a4e48f2`.
- Próxima ação vinculada: eventual aceite remoto/HTTPS ou desenho de shell deve chegar como missão separada com ID/contrato próprio; não inferir que a aprovação READ + WRITE + GIT autoriza execução.

### Reconciliação SS-MVP-002 — publicação remota confirmada — 23/09/2026

- O registro anterior de promoção local foi produzido antes do push. Depois dele, `e97aaf7ec84577f2999f0bb725db5d2b8a451ce2` e `d1bff2c925e83061bcbc15cef299b22846ac897b` foram publicados por push normal, sem force-push; `git ls-remote` confirmou `origin/codex/mvp-vertical-programming` em `d1bff2c925e83061bcbc15cef299b22846ac897b`.
- A composição pública opt-in `connect quick programming` permanece limitada a READ + WRITE + Git observacional. `test.run`, shell, comandos arbitrários e mutações Git continuam fora do gate. A publicação remota não é aceite operacional HTTPS/ChatGPT Web e não conclui `SS-MVP-002`.
- Histórico anterior que registrava a ausência de push antes deste marco foi preservado como fato do momento; o estado corrente é local/remoto confirmado em `d1bff2c`.
- Estados correntes: `SS-MVP-002` **PARCIAL**, `SS-BE-007` **PARCIAL**, `SS-MVP-002-PROMOTION-GATE-001` **APROVADO PELO PROPRIETÁRIO / implementação local e publicação remota confirmadas**, CI **DESCONHECIDA**.
- Próxima ação vinculada: aceite operacional externo de READ + WRITE + GIT ou contrato separado de shell, somente mediante missão/autorização específica; não inventar nova feature.


### Atualização SS-MVP-002 — direção de coding agent completo: filesystem, Git e UX — 23/09/2026

- O proprietário aprovou como direção de produto que o ChatGPT Web possa agir como um coding agent completo no workspace autorizado, evoluindo além de `replace_text`/Git review para filesystem tipado completo, Git operacional em camadas e UX específica por domínio/tool.
- Filesystem alvo: inspeção/busca (`stat_path`, `list_directory`, `read_file`, `find_paths`, `search_text`), criação/edição (`create_directory`, `create_text_file`, `replace_text`, `write_text_file`), estrutura (`copy_path`, `move_path`), remoção explícita (`delete_file`, `delete_directory`) e, depois, `apply_patch` sobre o mesmo motor seguro. Artifacts/binários permanecem superfície separada.
- Git alvo: manter `signalspace:git.review` observacional; desenhar capacidade independente para mutações locais tipadas (stage/unstage/branch/switch/commit), neutralizando hooks/configuração executável; tratar fetch/push como fronteira separada. Reset hard, clean, force-push e equivalentes não são autorizados por esta decisão.
- UX alvo: resultados estruturados estáveis para todas as tools e UI especializada quando trouxer valor (workspace, árvore/busca, diff/review, teste/processo, artifacts), sem exigir widget pesado por chamada e sem tornar o frontend autoridade de segurança.
- **Não implementado por este registro:** nenhuma nova tool, escopo OAuth, mutação Git, shell, `test.run`, túnel, OAuth externo, workspace real, CI, merge ou deploy. `SS-MVP-002` permanece **em andamento/PARCIAL**.
- Próximo slice recomendado e vinculado ao ID existente `SS-MVP-002`: consolidar o motor seguro de filesystem e adicionar primeiro `stat_path`, `find_paths`, `search_text`, `create_directory`, `create_text_file` e `write_text_file` com resultados estruturados, preservando as regressões READ/WRITE/GIT atuais. Copy/move/delete/apply_patch e Git mutável entram somente depois dos gates dessa base.

### Atualização SS-MVP-002 — filesystem tipado base reconciliado — 23/09/2026

- A missão recuperou a branch com alterações locais do slice, preservou os dois patches não rastreados e executou `git fetch origin --prune` seguido de fetch explícito da branch porque a configuração local de fetch não atualizava `origin/codex/mvp-vertical-programming`. O remoto live e o tracking agora apontam para `1cf598a5222a8118d268752dbf2bbd0993f437ff`.
- O código e os testes foram separados em um commit de implementação (`e39d7d9`, `feat(signalspace): add typed workspace filesystem tools`); a documentação/checkpoint/direção de worktrees foi separada em um segundo commit documental (HEAD desta missão). Ambos foram rebaseados sobre o remoto sem merge e sem force-push. Os conflitos documentais foram reconciliados preservando a direção de coding agent completo do remoto e acrescentando o estado factual dos seis tools implementados.
- Passaram os gates focais, `go test ./... -p=1 -parallel=1 -count=1 -timeout=300s`, race serial de `internal/workspace`, `internal/mcp` e `cmd/signalspace`, `go vet ./...`, `go build ./...`, `gofmt -l cmd internal` e `git diff --check`. Não houve CI, push, merge, deploy, túnel, OAuth externo, workspace real ou worktree funcional.
- Subagents não foram utilizados: não havia runtime auxiliar independente disponível que trouxesse ganho material sem duplicar a revisão; a validação final foi executada e reconciliada pelo agente principal. A direção de worktrees continua apenas documental e a próxima escolha A/B/C não foi iniciada.

### Atualização SS-MVP-002 — filesystem estrutural local — 23/09/2026

- A ref foi revalidada antes da implementação: `codex/mvp-vertical-programming`, HEAD e remoto live em `6b8adc5b44e4856a743f0df336241aff5b5da79e`; os dois patches protegidos permaneceram não rastreados, não aplicados, intocados e com os hashes preservados.
- Foram implementadas `copy_path`, `move_path`, `delete_file` e `delete_directory` sob `signalspace:workspace.write`, reutilizando `Session`/`Grants`. Copy limita profundidade/entradas/bytes e reporta cleanup/partial; move usa no-replace e rejeita EXDEV; delete não recursa e não remove a raiz. `apply_patch`, shell, `test.run`, Git mutável, worktree, UI e artifacts continuam fora.
- A composição programming exige e registra as quatro portas estruturais; `diagnostic` e `read` não as anunciam. Testes cobrem regressões READ/WRITE/GIT, negativos MCP, traversal, symlink, tipos, limites, cleanup parcial, destino existente, diretório não vazio, owner/client/session, escopo e revoke. EXDEV não foi exercitado por ausência de fixture cross-device.
- Estado: `SS-MVP-002` **IMPLEMENTADA E VALIDADA LOCALMENTE / PARCIAL NO ACEITE OPERACIONAL EXTERNO**; implementação no commit local `347f84b`; `SS-BE-007` **PARCIAL**; `SS-MVP-002-PROMOTION-GATE-001` **APROVADO PELO PROPRIETÁRIO PARA READ + WRITE + GIT OBSERVACIONAL**; CI **DESCONHECIDA**. O commit documental será criado agora; push requer missão/autorização posterior.
